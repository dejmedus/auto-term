{
  "setup": [
    {
      "tab": "frontend",
      "commands": ["*echo install stuff"],
      "icon": "browser"
    }
  ],
  "open": [
    {
      "tab": "git",
      "commands": ["git branch"],
      "icon": "git"
    },
    {
      "tab": "frontend",
      "commands": ["*echo start the app"],
      "icon": "browser"
    },
    {
      "tab": "test",
      "commands": ["*echo test command"],
      "icon": "beaker"
    }
  ],
  "start": [
    {
      "tab": "frontend",
      "commands": ["*stop", "clear", "*echo restart the app"]
    }
  ],
  "close": [
    {
      "tab": "git",
      "commands": ["*close"]
    },
    {
      "tab": "frontend",
      "commands": ["*stop", "*close"]
    }
  ]
}
